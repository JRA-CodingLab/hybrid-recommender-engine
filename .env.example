# Hybrid Recommender Engine — Environment Variables
# Copy this to .env and adjust as needed.

# Redis (optional — falls back to in-memory cache if unavailable)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# API
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=false
